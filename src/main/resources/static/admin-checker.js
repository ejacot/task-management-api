(()=>{
 let data,management,adminLoaded=false,employees=[];
 const $=s=>document.querySelector(s),money=n=>new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}).format(n||0),auth=()=>sessionStorage.getItem('roomly.auth');
 const today=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
 const api=async(path,options={})=>{const response=await fetch(path,{...options,headers:{'Content-Type':'application/json',Authorization:`Basic ${auth()}`}});if(!response.ok){let body={};try{body=await response.json()}catch{}throw new Error(body.message||`Eroare ${response.status}`)}return response.status===204?null:response.json()};
 const toast=message=>{const el=$('#toast');el.textContent=message;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2200)};
 const roleName=role=>({EMPLOYEE:'Angajat',CHECKER:'Checker',MANAGER:'Manager',EMPLOYER:'Angajator'}[role]||role);
 function bindOnce(){if(adminLoaded)return;adminLoaded=true;$('#checker-date').value=today();$('#checker-date').addEventListener('change',loadChecker);$('#new-employee').addEventListener('click',()=>openEmployee());$('#close-employee-dialog').addEventListener('click',()=>$('#employee-dialog').close());$('#employee-form').addEventListener('submit',saveEmployee);$('#hotel-settings-form').addEventListener('submit',saveHotelSettings);$('#admin-employees').addEventListener('click',employeeAction);$('#checker-rooms').addEventListener('click',checkerAction);$('#admin-work-types').addEventListener('submit',saveWorkType);}
 async function loadAdmin(){if(!['MANAGER','EMPLOYER'].includes(data.me.role))return;const [list,settings]=await Promise.all([api('/api/admin/employees'),api('/api/admin/hotel-settings')]);employees=list.employees;renderEmployees();renderHotelSettings(settings);renderWorkTypes();}
 async function loadChecker(){if(!['CHECKER','MANAGER','EMPLOYER'].includes(data.me.role))return;const day=$('#checker-date').value||today(),summary=await api(`/api/checker/rooms?date=${day}`);$('#checker-summary').innerHTML=`<span><strong>${summary.total}</strong> camere</span><span><strong>${summary.checked}</strong> verificate</span><span><strong>${summary.defects}</strong> probleme</span><span><strong>${summary.released}</strong> eliberate</span>`;$('#checker-rooms').innerHTML=summary.rooms.map(room=>`<article class="checker-room status-${room.status}"><div><h3>Camera ${room.roomNumber}</h3><p>${room.employee} · ${room.category} · <span class="settings-pill">${room.status}</span></p>${room.defectDescription?`<p>Problemă: ${room.defectDescription}</p>`:''}${room.checkedBy?`<p>Verificat de ${room.checkedBy}</p>`:''}</div><div class="checker-actions"><button class="small-btn good" data-room="${room.id}" data-status="CHECKED">Verificat</button><button class="small-btn warn" data-room="${room.id}" data-status="DEFECT">Defect</button><button class="small-btn" data-room="${room.id}" data-status="RELEASED">Eliberat</button></div></article>`).join('')||'<p class="muted">Nu există camere repartizate în această zi.</p>'}
 function renderEmployees(){$('#admin-employees').innerHTML=employees.map(e=>`<article class="admin-person ${e.active?'':'inactive'}"><div><h3>${e.displayName}</h3><p>${roleName(e.role)} · ${e.teamName||e.hotel||'fără echipă'} · ${money(e.hourlyRate)}/h</p><p>${e.email||'fără email'} · ${e.phone||'fără telefon'}</p>${e.invitationLink?`<p class="invite-link">Invitație: ${location.origin}${e.invitationLink}</p>`:''}</div><div class="admin-actions"><button class="small-btn" data-edit="${e.id}">Editează</button><button class="small-btn" data-invite="${e.id}">Invită</button><button class="small-btn ${e.active?'bad':'good'}" data-active="${e.id}" data-value="${!e.active}">${e.active?'Dezactivează':'Activează'}</button></div></article>`).join('')||'<p class="muted">Nu există angajați.</p>'}
 function renderHotelSettings(s){$('#hotel-settings-form').innerHTML=`<label>Nume hotel</label><input id="set-name" value="${s.name||''}" required><label>Oraș</label><input id="set-city" value="${s.city||''}" required><div class="field-grid"><div><label>Normal camere/oră</label><input id="set-normal" type="number" step="0.01" value="${s.normalRoomsPerHour}"></div><div><label>Junior camere/oră</label><input id="set-junior" type="number" step="0.01" value="${s.juniorRoomsPerHour}"></div><div><label>President camere/oră</label><input id="set-president" type="number" step="0.01" value="${s.presidentRoomsPerHour}"></div><div><label>Pauză standard</label><input id="set-break" type="number" value="${s.defaultBreakMinutes}"></div><div><label>Duminică %</label><input id="set-sunday" type="number" step="0.01" value="${s.sundayPremiumPercent}"></div><div><label>Noapte %</label><input id="set-night" type="number" step="0.01" value="${s.nightPremiumPercent}"></div><div><label>Sărbătoare %</label><input id="set-holiday" type="number" step="0.01" value="${s.holidayPremiumPercent}"></div></div><button class="btn primary wide" ${data.me.role!=='EMPLOYER'?'disabled':''}>Salvează setările</button>${data.me.role!=='EMPLOYER'?'<p class="muted">Doar angajatorul modifică banii.</p>':''}`}
 function renderWorkTypes(){$('#admin-work-types').innerHTML=data.workTypes.map(t=>`<form class="work-type-row" data-type="${t.id}"><input name="code" value="${t.code}" required><input name="name" value="${t.name}" required><select name="unit"><option value="HOURLY" ${t.unit==='HOURLY'?'selected':''}>Ore</option><option value="ROOMS" ${t.unit==='ROOMS'?'selected':''}>Camere</option></select><input name="roomsPerHour" type="number" step="0.01" placeholder="camere/oră" value="${t.roomsPerHour||''}"><input name="color" type="color" value="${t.color||'#246F60'}"><button class="small-btn">Salvează</button><input name="start" type="time" value="${t.defaultStartTime?.slice(0,5)||''}"><input name="end" type="time" value="${t.defaultEndTime?.slice(0,5)||''}"><input name="breakMinutes" type="number" min="0" value="${t.defaultBreakMinutes||0}"><label><input name="active" type="checkbox" checked> activ</label></form>`).join('')}
 function openEmployee(employee){$('#employee-form').reset();$('#employee-id').value=employee?.id||'';$('#employee-dialog-title').textContent=employee?'Editează angajat':'Angajat nou';$('#employee-username').value=employee?.username||'';$('#employee-role').value=employee?.role||'EMPLOYEE';$('#employee-role').querySelectorAll('option').forEach(option=>{option.disabled=data.me.role!=='EMPLOYER'&&['MANAGER','EMPLOYER'].includes(option.value)});$('#employee-first').value=employee?.firstName||'';$('#employee-last').value=employee?.lastName||'';$('#employee-email').value=employee?.email||'';$('#employee-phone').value=employee?.phone||'';$('#employee-rate').value=employee?.hourlyRate||'16.00';$('#employee-rate').disabled=data.me.role!=='EMPLOYER';$('#employee-tax').value=String(employee?.steuerClass||1);$('#employee-address').value=employee?.address||'';$('#employee-team').value=employee?.teamName||'';$('#employee-temp-password').value='';$('#employee-error').textContent=data.me.role!=='EMPLOYER'?'Managerul poate modifica datele și echipa, dar nu banii.':'';$('#employee-dialog').showModal()}
 async function saveEmployee(event){event.preventDefault();const id=$('#employee-id').value,body={username:$('#employee-username').value,role:$('#employee-role').value,firstName:$('#employee-first').value,lastName:$('#employee-last').value,email:$('#employee-email').value||null,phone:$('#employee-phone').value||null,hourlyRate:Number($('#employee-rate').value||0),steuerClass:Number($('#employee-tax').value||1),address:$('#employee-address').value||null,teamName:$('#employee-team').value||null,temporaryPassword:$('#employee-temp-password').value||null};try{await api(id?`/api/admin/employees/${id}`:'/api/admin/employees',{method:id?'PUT':'POST',body:JSON.stringify(body)});$('#employee-dialog').close();await loadAdmin();toast('Angajat salvat')}catch(error){$('#employee-error').textContent=error.message}}
 async function employeeAction(event){const edit=event.target.dataset.edit,active=event.target.dataset.active,invite=event.target.dataset.invite;if(edit)openEmployee(employees.find(e=>e.id===Number(edit)));if(active){await api(`/api/admin/employees/${active}/active`,{method:'PUT',body:JSON.stringify({active:event.target.dataset.value==='true'})});await loadAdmin();toast('Status actualizat')}if(invite){const response=await api(`/api/admin/employees/${invite}/invite`,{method:'POST'});await navigator.clipboard?.writeText(location.origin+response.invitationLink).catch(()=>{});await loadAdmin();toast('Linkul de invitație a fost generat')}}
 async function saveHotelSettings(event){event.preventDefault();if(data.me.role!=='EMPLOYER')return;const body={name:$('#set-name').value,city:$('#set-city').value,normalRoomsPerHour:Number($('#set-normal').value),juniorRoomsPerHour:Number($('#set-junior').value),presidentRoomsPerHour:Number($('#set-president').value),sundayPremiumPercent:Number($('#set-sunday').value),nightPremiumPercent:Number($('#set-night').value),holidayPremiumPercent:Number($('#set-holiday').value),defaultBreakMinutes:Number($('#set-break').value)};await api('/api/admin/hotel-settings',{method:'PUT',body:JSON.stringify(body)});toast('Setările hotelului au fost salvate')}
 async function saveWorkType(event){event.preventDefault();const form=event.target;if(!form.dataset.type)return;const body={code:form.code.value,name:form.name.value,unit:form.unit.value,roomsPerHour:form.roomsPerHour.value?Number(form.roomsPerHour.value):null,color:form.color.value,active:form.active.checked,defaultStartTime:form.start.value||null,defaultEndTime:form.end.value||null,defaultBreakMinutes:Number(form.breakMinutes.value||0)};await api(`/api/admin/work-types/${form.dataset.type}`,{method:'PUT',body:JSON.stringify(body)});toast('Tipul de task a fost salvat')}
 async function checkerAction(event){const id=event.target.dataset.room,status=event.target.dataset.status;if(!id)return;const defect=status==='DEFECT'?prompt('Ce problemă are camera?')||'Problemă raportată':null;await api(`/api/checker/rooms/${id}`,{method:'PUT',body:JSON.stringify({status,notes:null,defectDescription:defect})});await loadChecker();toast('Camera a fost actualizată')}
 window.setupAdminChecker=async(current,overview)=>{data=current;management=overview;bindOnce();await Promise.all([loadChecker(),loadAdmin()])};
})();
