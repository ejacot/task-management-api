(()=>{
 let initialized=false;
 const $=value=>document.querySelector(value),auth=()=>sessionStorage.getItem('roomly.auth');
 const today=()=>{const value=new Date();return `${value.getFullYear()}-${String(value.getMonth()+1).padStart(2,'0')}-${String(value.getDate()).padStart(2,'0')}`};
 async function load(){const date=$('#my-rooms-date').value,response=await fetch(`/api/employee/rooms?from=${date}&to=${date}`,{headers:{Authorization:`Basic ${auth()}`}}),rooms=await response.json();$('#my-rooms-list').innerHTML=rooms.length?`<div class="room-assignment-list">${rooms.map(room=>`<span><strong>${room.roomNumber}</strong><small>${{NORMAL:'Normal',JUNIOR:'Junior Suite',PRESIDENT:'President'}[room.category]||room.category}</small></span>`).join('')}</div><p class="muted">${rooms.length} camere repartizate</p>`:'<p class="muted">Managerul nu ți-a repartizat camere pentru această zi.</p>'}
 window.setupEmployeeRooms=data=>{if(data.me.role!=='EMPLOYEE'||initialized)return;initialized=true;const nav=$('.sidebar nav');nav.insertAdjacentHTML('beforeend','<button class="nav employee-rooms-nav"><i>▦</i>Camerele mele</button>');$('.main').insertAdjacentHTML('beforeend',`<section id="employee-rooms-view" class="view"><div class="page-head"><div><p class="kicker">LISTA ZILEI</p><h1>Camerele mele</h1><p class="muted">Lista repartizată de manager pentru ziua selectată.</p></div><input id="my-rooms-date" type="date" value="${today()}"></div><section class="card"><div id="my-rooms-list"></div></section></section>`);$('.employee-rooms-nav').onclick=()=>{document.querySelectorAll('.view').forEach(view=>view.classList.toggle('active-view',view.id==='employee-rooms-view'));document.querySelectorAll('.nav').forEach(item=>item.classList.toggle('active',item.classList.contains('employee-rooms-nav')));load()};$('#my-rooms-date').onchange=load;};
})();
